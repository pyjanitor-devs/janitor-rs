# Keep Clippy's default argument threshold. Macro-generated Python functions
# opt out locally because their separate parameters are part of the public
# Python API; handwritten Rust functions remain subject to the default lint.
